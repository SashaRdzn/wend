# Скопируйте в frontend/.env.local для локальной проверки прод-сборки (не коммитится)

# URL бэкенда на Render (без слэша в конце). В Vercel задайте то же в Project → Environment Variables.
# VITE_API_URL=https://your-service.onrender.com
