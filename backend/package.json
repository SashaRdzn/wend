{
  "name": "wedding-backend",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.5.0"
  },
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:lan": "npx cross-env HOST=0.0.0.0 tsx watch src/index.ts",
    "build": "npm ci",
    "build:all": "npm ci --prefix ../frontend && npm run build --prefix ../frontend && npm ci",
    "start": "cross-env HOST=0.0.0.0 tsx src/index.ts",
    "start:with-spa": "cross-env HOST=0.0.0.0 NODE_ENV=production SERVE_SPA=1 tsx src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "cross-env": "^7.0.3"
  }
}
